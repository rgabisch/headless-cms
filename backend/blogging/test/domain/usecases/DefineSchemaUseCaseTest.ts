suite('Define Schema Use Case', () => {
    suite('when execute', () => {

        test('given empty name -> throw exception for empty name.');
        test('given name made of whitespaces -> throw exception for empty name.');
        test('given name with more than 50 characters -> throw exception for to long name.');
        test('given unassigned creator id -> throw exception for a empty unassigned id.');

    })
});